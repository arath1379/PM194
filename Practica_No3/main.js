// realizamos la importacion del modulo
const { restar } = require('./utils');

// Pruebas
console.log(restar(10, 5));    // 5
console.log(restar(3, 7));     // -4
console.log(restar(0, 0));     // 0
console.log(restar(100, 25));  // 75
console.log(restar(-10, -5));  // -5
